// LightboxControls — controls for lightbox navigation
// TODO: wire to LightboxGallery state (client-side only)
import React from "react";

const LightboxControls: React.FC = () => <div className="p-1">⟨ Prev | Next ⟩</div>;

export default LightboxControls;
