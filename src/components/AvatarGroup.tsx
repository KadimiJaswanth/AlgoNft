// AvatarGroup — small avatar group display
// TODO: show tooltips and optional click handlers (client-only)
import React from "react";

const AvatarGroup: React.FC = () => <div className="flex -space-x-2">Avatars</div>;

export default AvatarGroup;
