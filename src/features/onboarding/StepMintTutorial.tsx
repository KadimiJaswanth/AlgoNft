// StepMintTutorial — onboarding step showing mint tutorial
// TODO: show brief steps and link to MintPlayground (client-only)
import React from "react";

const StepMintTutorial: React.FC = () => (
  <div className="p-4">StepMintTutorial (follow these steps to mint)</div>
);

export default StepMintTutorial;
