// useAlgorandUnsignedTx — helper to build unsigned algosdk tx objects
// TODO: build unsigned transactions only; call wallet signer in UI layer
export const buildUnsignedPayment = (opts: any): any => {
  // Example: use algosdk to build tx (commented)
  // const txn = algosdk.makePaymentTxnWithSuggestedParamsFromObject({...})
  return {} as any;
};
