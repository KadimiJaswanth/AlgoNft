// StepShareCollection — onboarding step to share a collection
// TODO: implement social share buttons client-side (no backend tokens)
import React from "react";

const StepShareCollection: React.FC = () => (
  <div className="p-4">StepShareCollection (share your collection)</div>
);

export default StepShareCollection;
