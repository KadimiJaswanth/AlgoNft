// GalleryGridClean — clean grid layout for NFTs
// TODO: integrate lazy-loading of images and client-side filters
import React from "react";

const GalleryGridClean: React.FC = () => (
  <div className="grid grid-cols-3 gap-4">GalleryGridClean (placeholder)</div>
);

export default GalleryGridClean;
