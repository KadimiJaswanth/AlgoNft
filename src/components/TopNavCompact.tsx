// TopNavCompact — compact top navigation bar
// TODO: wire search and wallet connect button here
import React from "react";

const TopNavCompact: React.FC = () => (
  <nav className="flex items-center justify-between p-3">TopNavCompact (placeholder)</nav>
);

export default TopNavCompact;
